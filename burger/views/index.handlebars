<div class="container">
	<div>
		<h1>EAT DA BURGER!</h1>
	</div>
	<div class="row">
		<div class="col-md-6">
			{{#each burger_data}}
			{{unless devoured}}
				<div>
					<div>{{id}}, {{burger_name}}</div>
				</div>
				<div class="devBtn">
					<form method="POST" action="/update?_method=POST">
						<input type="hidden" type="text" name="id" value={{id}}>
						<button type="submit" class="btn btn-default">EA DA BURGER!</button>
					</form>	
				</div>
			{{/unless}}
			{{/each}}
		</div>
		<div class="col-md-6">
			{{#each burger_data}}
			{{if devoured}}
				<div>
					<div>{{id}}, {{burger_name}}</div>
				</div>
			{{/if}}
			{{/each}}
		</div>
	</div>
	<div class="row">
		<div class=col-md-12>
			<form action="/create" method="POST">
			  <input placeholder="Enter Burger Name" name="burger_name"></input><br>
			  <button type="submit" id= "addBurgBtn"class="btn btn-primary">Add a Burger!</button>
			</form>
		</div>
	</div>
</div>