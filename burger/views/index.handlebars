<div>
	<h1>EAT DA BURGER!!</h1>
</div>

<div>
	<div>
		{{each burger}}
		{{#if burger_name}}
			<div class="container">
				<div class="row">
					<div class="col-md-6">
						<pre>{{this.id}}, {{this.burger_name}}, {{this.date}}</pre>
					</div>
				</div>
			</div>
		{{/if}}
		{{/each}}	
	</div>
</div>